{
  "dependencies": {
    "concurrently": "^5.0.0"
  },
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm i && nodemon node ./bin/www",
    "dev": "concurrently \"npm run client\" \"npm run server\""
  }
}
